.right .employee-count table{
    background: var(--color-white);
    width: 100%;
    border-radius: var(--card-border-radius);
    padding: var(--card-padding);
    text-align: center;
    box-shadow: var(--box-shadow);
    transition: all 300ms ease;
}

__________________________________

.right-top  {
    margin-top: 1.4rem;
}

.right-top .top{
    display: flex;
    justify-content: end;
    gap: 2rem;
    
}

.right-top .top button{
    display: none;
}


.right-top .theme-toggler{
    background: var(--color-light);
    display: flex;
    justify-content: space-around;
    align-items: center;
    height: 1.6rem;
    width: 4.2rem;
    cursor: pointer;
    border-radius: var(--border-radius-1);
}

.right-top .theme-toggler span{
    font-size: 1.2rem;
    width: 50%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
}

.right-top .theme-toggler span.active{
    background: var(--color-primary);
    color: white;
    border-radius: var(--border-radius-1);

}

.right-top .top .profile{
    display: flex;
    gap: 2rem;
    text-align: right;
}

/* ===== RIGHT EMPLOYEE COUNT ====== */

.right-container .employee-count {
    margin-top: 1rem;
}

.right-container .employee-count h2{
    margin-bottom: 0.8rem;
    text-align: center;
}

.right-container .employee-count .projects {
    background: var(--color-white);
    padding: var(--card-padding);
    border-radius: var(--card-border-radius);
    box-shadow: var(--box-shadow);
    transition: all 300ms ease;
}

.right-container .employee-count .projects:hover {
    box-shadow: none;
}

.right-container .employee-count table{
    background: var(--color-white);
    width: 100%;
    border-radius: var(--card-border-radius);
    padding: var(--card-padding);
    text-align: center;
    box-shadow: var(--box-shadow);
    transition: all 300ms ease;
}

.right-container .employee-count table:hover{
    box-shadow: none;
}

.right-container table tbody td{
    height: 2.8rem;
    border-bottom: 1px solid var(--color-light);
    color: var(--color-dark-variant);
}

.right-container table tbody tr:last-child td{
    border: none;

}

.right-container table tbody td:hover{
    color: var(--color-white-table-font);
    transition: all 300ms ease;
    border-left: none;
    border-right: none;
   
}


.right-container table tbody tr:hover td {
    background-color: var(--color-table-highlight);
    color: var(--color-white-table-font);
    cursor: pointer;
    transition: all 300ms ease;
    border-collapse: collapse;
    border-spacing: 0;
    border-spacing: 0;
}

.right-container .employee-count table{
    background: var(--color-white);
    width: 100%;
    border-radius: var(--card-border-radius);
    padding: var(--card-padding);
    text-align: center;
    box-shadow: var(--box-shadow);
    transition: all 300ms ease;
    
}

.project-employees  {
    margin-top: 1.4rem;
    display: flex;
    justify-content: center;
}
.main-layout {
    display: flex;
    align-items: flex-start;
    gap: 2rem;
    margin-top: 1.4rem;
  }

  .left-content {
    flex: 1;
  }

  .right-container {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    width: 300px;
  }